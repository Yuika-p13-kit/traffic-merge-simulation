# 高速道路合流制御シミュレーション プロジェクト概要

## 1. 目的

高速道路における主線・従線の合流で、どの戦略が渋滞や交通損失を最小化するかを交通流シミュレーションで検証する。

- 発想の起点: 首都高速3号渋谷線・大橋JCT下り付近（C2中央環状線からの合流）
- 目的は実道路の完全再現ではなく、主従関係を持つ合流問題の抽象化と検証
- 成果物は再現可能なコード（GitHub）と実験考察（Qiita）として公開する

## 2. 問題意識

合流部では、従線流入により主線減速が発生し、後続へ伝播して渋滞化する可能性がある。一方で主線優先を強めすぎると従線待ち行列が増大する。

検証する中心課題:

- 主線交通量と従線交通量が与えられたとき、道路全体の交通損失を最小化する戦略は何か

## 3. 対象モデル（初期）

大橋JCTの詳細形状は初期段階では再現せず、単純な主従合流モデルを扱う。

- 主線: 交通流をできるだけ乱したくない優先側
- 従線: 主線へ流入する側
- 大橋JCTモチーフの対応仮定:
  - 主線: 3号渋谷線下り
  - 従線: C2中央環状線からの流入
- 事故・工事・天候などは初期段階では非考慮

## 4. 研究課題（RQ）

### RQ1: 単純ルールの有効性

以下を比較し、交通状態の変化を確認する。

- 無制御
- 1:1 交互合流
- 主線2:従線1
- 主線3:従線1
- 主線5:従線1
- その他固定比率

### RQ2: 交通量条件による最適戦略の変化

主線交通量 $q_m$ と従線交通量 $q_s$ を変化させ、最適戦略の領域を可視化する。

- 目標: 主線交通量 × 従線交通量 → 最適戦略
- 可視化: ヒートマップ等

### RQ3: 動的制御の優位性

固定比率制御と、代表的フィードバック型 Ramp Metering である ALINEA を比較し、動的制御の価値を検証する。

## 5. 評価指標

主線平均速度のみの最適化は不適切解（従線を極端に抑制）を生むため、ネットワーク全体で評価する。

候補指標:

- Total Travel Time
- Total Time Spent
- 平均旅行時間
- 平均速度
- 主線側遅延時間
- 従線側遅延時間
- 従線待ち行列長
- 停止回数
- 急減速回数
- スループット

主指標の有力候補:

- Total Time Spent / Total Travel Time 系

副指標・制約候補:

- 従線の最大待ち時間
- 最大待ち行列長

## 6. 使用技術

### 交通シミュレーション

- Eclipse SUMO

採用理由:

- オープンソース
- 車両単位の microscopic simulation が可能
- 合流・車線変更を扱える
- Python から制御できる
- 交通研究で利用実績がある
- 公開時に再現しやすい

### シミュレーション制御

- Python + TraCI

取得・利用対象:

- 交通量
- 速度
- 占有率
- 待ち行列
- 車両状態

### 将来候補（初期では未導入）

- Optuna
- Bayesian Optimization
- Model Predictive Control
- Reinforcement Learning

## 7. 初期実験計画

### Step 1: 最小ネットワーク構築

- 主線・従線・合流部のみの SUMO ネットワークを構築
- 無制御で交通流を発生させ、閾値超過での渋滞形成を確認

### Step 2: 固定戦略比較

比較対象:

1. 無制御
2. 1:1
3. 2:1
4. 3:1
5. 5:1
6. 必要に応じた追加比率

### Step 3: 交通量スイープ

例:

- 主線交通量: 600 / 1000 / 1400 / 1800 / 2200 veh/h
- 従線交通量: 200 / 400 / 600 / 800 / 1000 veh/h

※ 実レンジはモデル構築後に調整する。

### Step 4: 可視化

- 主線交通量 × 従線交通量 × 最適戦略をヒートマップ化
- 「常に同一戦略が最適」ではなく、条件で最適が切り替わるかを重視

### Step 5: ALINEA 比較

比較順:

- 無制御
- 固定比率
- ALINEA

## 8. 将来的な発展

- Simulation Optimization（SUMO を評価関数とするブラックボックス最適化）
- ALINEA ゲイン・目標占有率・投入間隔などのパラメータ最適化
- MPC
- RL（状態・行動・報酬設計を含む）

※ RL を使うこと自体は目的にしない。

## 9. 基本方針

### 現実再現より問題構造理解を優先

- 初期は大橋JCTの完全再現をしない
- 必要時にのみ段階的に現実データ・形状へ近づける

### 手法は段階的に高度化

1. 無制御
2. 単純ルール
3. フィードバック制御
4. 数理最適化
5. 予測制御 / 機械学習

### 再現可能性重視

- 第三者が少ない手順で再実行できる構成を目指す

## 10. 公開成果物

### GitHub

- 実験コード・条件・結果を公開
- 目標: `uv run python experiments/sweep.py` 等で再実験可能

### Qiita

第一報テーマ（案）:

- 「高速道路の合流は交互が最適なのか？SUMOで主従合流をシミュレーションしてみる」

記事構成の基本流れ:

1. 問題意識（大橋JCT）
2. 問題の単純化
3. SUMO再現
4. 複数ルール比較
5. 交通量別可視化
6. 結果考察
7. ALINEA比較

## 11. 現時点での確定事項

- テーマは高速道路の主従合流制御
- 大橋JCT下りを発想起点とする
- 実道路完全再現からは始めない
- SUMO を使用する
- Python から実験・制御する
- 複数交通量条件を比較する
- 無制御・固定ルール・ALINEA を主要比較対象とする
- GitHubでコード公開する
- Qiitaで記事化する
- 再現可能な実験を目指す

## 12. 未確定事項（今後決定）

- SUMO上の道路形状
- 主線・従線の車線数
- 合流車線長
- 車両発生モデル
- シミュレーション時間
- 暖機時間
- 乱数 seed 数
- 評価指標の最終選定
- 交通量レンジ
- 固定比率制御の実装詳細
- ALINEA実装・パラメータ
- 実データ利用有無
- 最適化を第一報に含めるか
- Qiitaの単発/シリーズ構成

## 13. 避けること

- 初手から大橋JCT完全再現を目指して停滞する
- 高度手法を使うこと自体を目的化する
- 指標を平均速度だけにする
- 主線のみ評価し従線待ちを無視する
- 単発シミュレーションで結論化する
- 条件・seed を記録せず再現不能にする
- 結果に合わせて評価条件を変える

## 14. 第一段階の成功条件

1. SUMO上に主従合流モデルがある
2. 無制御の基準結果を取得できる
3. 複数固定戦略を比較できる
4. 主線・従線交通量を複数条件で変更できる
5. 結果をCSV等で自動取得できる
6. 戦略ごとの差をグラフ/ヒートマップで示せる
7. 第三者が再現できる
8. GitHub公開できる
9. Qiita記事として整理できる

---

最終更新: 2026-08-16
記録者: GitHub Copilot