[project]
name = "traffic-merge-simulation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "traci>=1.27.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
