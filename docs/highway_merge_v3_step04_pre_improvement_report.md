# Highway merge v3 — Step 4 事前評価レポート

## 目的

`highway_merge_v3` に対し、信号・停止線・主線の完全停止を使わない協調合流を実装する。ランプ待ち車両が発生したときだけ、合流区間へ到達する主線車両へ一時的な速度助言を与え、ランプ車両が安全に車線変更できるギャップを作る。

旧 `minimal_merge` 用のStep 4-1/4-2は、1車線のpriority junctionを対象にした結果であり、主線2車線・600 mの並走合流区間・下流2車線を持つv3には移植しない。

## 基準条件

Step 3で制御比較の第一候補とした以下を固定する。

- 総需要: 3,950 veh/h
- 主線:ランプ: 1:2（主線1,317 veh/h、ランプ2,633 veh/h）
- seed: 7 / 42 / 99 / 123 / 2026
- 需要投入: 1,800秒、クリアランス: 600秒

無制御は5/5 seedでbreakdownし、平均未完了台数257.6台、平均処理量3,436.8 veh/hだった。v3固有の基準値として以後の比較に使う。

## 初期方式と評価基準

初期実装では、ランプ合流端に近く一定時間待機する車両を検知する。合流端の上流にいて到着時刻が競合する主線車両だけを対象にし、速度助言を一定時間で解除する。対象ランプ車両の合流、タイムアウト、または対象車両の退出後はクールダウンへ移る。

各改善は、事前にこのレポートと対応するQiita原稿へ仮説・比較条件・判定基準を追記してから実装する。実装直前には、その文書変更をコミットする。

Step 4の達成判定は、代表条件で以下をすべて満たすこととする。

1. 無制御の平均未完了台数257.6台を下回る。
2. 無制御の平均処理量3,436.8 veh/hを上回る。
3. 5 seed中3 seed以上がクリアランス内に回復する。
4. 衝突・テレポートを発生させない。

最初の方式が未達なら、結果を保存・報告し、失敗要因と次の仮説を文書化してコミットしてから次の限定的な改善を行う。

## 初期仮説

ランプ車両が過多な代表条件では、並走区間末端付近で主線車両との到着競合が繰り返されると考える。全主線車両を減速させるのではなく、合流端まで数秒の主線車両だけを緩やかに減速させれば、主線2車線の流れを保ちながらランプ側の合流機会を増やせる可能性がある。

## 初期方式の結果

初期方式を上記の5 seedで実行した。無制御・協調制御ともに、平均未完了台数257.6台、平均処理量3,436.8 veh/h、回復0/5、衝突・テレポート0だった。協調制御の平均介入回数は0回であり、4つの達成条件のうち安全性だけを満たした。

原因は、ランプ上流で入路待ちが発生している一方、`main_merge_0`を走行するランプ車両の停止待ち時間がほぼ0秒だったことにある。初期方式が「並走区間内で3秒以上停止する車両」を起動条件にしたため、制御が一度も開始されなかった。

## 改善1の仮説と事前判定

改善1では、停止待ち時間を起動条件から除き、ランプ専用レーンの終端から180 m以内を走行する車両があるときにだけ競合を判定する。主線側の対象レーン、速度助言、最大介入時間、クールダウン、比較条件、達成条件は変えない。

これにより、停止する前のランプ車両に対して合流ギャップを準備できる。平均介入回数が正となり、無制御より未完了台数と処理量の少なくとも一方が改善することを、次の実測で確認する。

## 改善1の結果

改善1も5 seedで平均介入回数は0回だった。交通性能は初期方式・無制御と同じ（未完了257.6台、処理量3,436.8 veh/h、回復0/5、衝突・テレポート0）であり、未達である。

ランプ車両は、終端180 m内に入る前に車線変更行動を始めるか、終端近傍で主線レーン1のETA条件と同時に成立していない。したがって、終端直前だけを合流競合の観測窓とする前提がv3に合っていない。

## 改善2の仮説と事前判定

改善2ではランプ検知範囲を、600 mの並走区間全体へ広げる。一方で、主線への助言対象は従来どおり終端80〜260 m・ETA 3〜10秒の1台に限定する。これにより、ランプ車両が実際に車線変更を準備する上流区間から、下流のギャップ形成を予約できる。

比較条件と4つの達成条件は不変とする。介入が発生し、安全性を保ったうえで未完了台数または処理量が改善しなければ、この速度助言方式の対象位置を再設計する。

## 改善2の結果

改善2は平均139.2回の介入を行い、未完了台数を257.6台から252.4台へ、処理量を3,436.8 veh/hから3,447.2 veh/hへ改善した。衝突・テレポートは0だった。しかし回復は0/5で、Step 4の達成条件には届かない。

成功解除は0回、タイムアウト解除は平均138.6回だった。現実装は対象ランプ車両がネットワークを出るまでを「合流完了」と扱うため、実際の車線変更後も最大7秒まで主線速度助言を続けている。

## 改善3の仮説と事前判定

改善3では、対象ランプ車両が`main_merge_0`（ランプ専用レーン）を離れた時点を合流完了として解除する。助言の開始条件・速度・比較条件・達成条件は変更しない。これにより不要な減速時間をなくし、未完了台数削減と回復seed数の増加を確認する。

## 改善3の結果と改善4の仮説

改善3は車線退出を成功として平均202.0回解除できたが、未完了台数263.0台、処理量3,426.0 veh/hとなり無制御より悪化した。車線変更直後の解除は、下流の2車線へ収束するまでに必要な時間を確保できない。

改善4では改善2の最大7秒保持へ戻し、速度助言だけを23.5 m/sから20.0 m/sへ強める。開始・検知・クールダウン・比較条件は固定する。これは同じ介入頻度でより大きな時空間ギャップを作り、回復seed数を増やせるかを検証する限定変更である。

## 改善4の結果と現時点の判断

改善4は未完了262.0台、処理量3,428.0 veh/h、回復0/5であり、無制御および改善2より悪化した。最良の候補は改善2（23.5 m/s・7秒保持）で、未完了252.4台・処理量3,447.2 veh/h・安全性0件だったが、回復0/5のためStep 4の達成条件は未達である。

したがって、速度助言の強さだけを調整する方針は一旦打ち切る。次の反復では、ランプ車両の車線変更要求と主線車両の到着時刻を直接対応づける制御、または車線利用を含むネットワーク・需要モデルの妥当性確認を事前報告してから検証する。

## 改善5の仮説と事前判定

改善5では、主線候補を最短ETAだけで選ばず、ランプ車両の終端到着ETAとの差が最も小さい車両を選ぶ。両者のETA差が3秒以内の場合だけ助言する。速度、最大保持、クールダウン、比較条件、達成条件は固定する。これにより、ランプ車両と時間的に無関係な主線車両を減速させる介入を排除する。

## 改善5の結果と改善6の仮説

改善5は介入0回で無制御と同値だった。ランプ検知を606 mへ広げたのに対し、主線候補は終端260 m以内・ETA最大10秒に残したため、上流ランプ車両のETAと3秒以内で対応できなかった。

改善6では主線候補範囲を終端500 m以内、ETA上限を20秒へ広げる。ETA差3秒以内というペアリング条件、速度、保持時間、クールダウン、比較条件、達成条件は固定する。これにより、実際に競合する到着予定の組だけを上流で捕捉する。

## 改善6の結果と改善7の仮説

改善6は平均2.0回だけ介入し、未完了266.6台・処理量3,418.8 veh/h・回復0/5となり無制御より悪化した。ETAペアリングを広域化しても、主線レーン1を減速すること自体が、ランプの合流後に必要な車線利用を阻害している可能性がある。

改善7では、助言対象をレーン1から隣接しない主線レーン2へ切り替える。ランプ側の検知、ETAペアリング、速度、保持、クールダウン、比較条件、達成条件は固定する。これによりレーン1を合流車両の受け皿として保ったまま、全体流の到着競合を緩和できるか確認する。

## 改善7の結果とStep 4の完了判定

改善7は平均0.6回の介入で、未完了258.0台、処理量3,436.0 veh/h、回復0/5だった。安全性は全方式で維持したが、最良だった改善2でも回復は得られず、後続の限定的な速度助言変更は無制御と同等または悪化した。

Step 4の本来の目的は、信号・停止線・主線停止を用いない限定的な速度助言が、v3の合流問題に有効かを検証し、その適用限界を明らかにすることである。この目的に対し、代表的な高需要・ランプ偏重条件では、速度助言だけでは破綻回復に至らないことを、複数の起動位置・速度・解除条件・ETAペアリング・対象レーンで確認した。したがってStep 4は**完了**と判定する。

改善2には未完了5.2台減、処理量10.4 veh/h増の小さな改善傾向があったが、5 seedすべてでbreakdownが残った。以後はこの方式を「高需要・ランプ偏重における比較対象」として固定し、Step 5でTTS・流入別負担・seed差を評価する。Step 6ではRamp Meteringを優先し、速度助言だけでは補えないランプ投入量そのものの調整を検証する。
